export class Transpiler {
  //load export schema or method
  // -> JSON schemas => mongodb
  // -> JSON schemas => vectordb
  // -> CMDL extraction with filters => materials only, reactions, materials + char data
  //export document(s)
  //log any errors
  //toJSON(exports: unknown[] schema: JSONSchema)
  // base json => metadata => record information => one metadata per file
  // iterate through export array and dump into schema?
  // fullRecord => standard
  // materialRecord => product, structure, properties
}
