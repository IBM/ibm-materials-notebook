import { SymbolTableBuilder } from "../symbol-table-builder";

describe("Unit tests for symbol table builder functions", () => {
  it("initializes correctly", () => {});
  it("visits collections", () => {});
  it("visits properties", () => {});
});
