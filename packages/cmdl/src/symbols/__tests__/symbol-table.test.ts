import { SymbolTable } from "../symbol-table";

describe("Unit tests for selected SymbolTable functions", () => {
  it("initializes correctly", () => {});
  it("adds a nested scope", () => {});
  it("adds a symbol", () => {});
  it("performs a basic lookup", () => {});
  it("peforms a lookup with a path", () => {});
  it("determines if symbol exists on current scope", () => {});
  it("can retrieve a symbol", () => {});
  it("throws an error if symbol is not found", () => {});
  it("can remove stale symbols", () => {});
  it("can search symbols based on a query", () => {});
});
