import { ErrorTable } from "../error-manager";

describe("Unit tests for Error Table", () => {
  it("initializes correctly", () => {});
  it("adds new error entries", () => {});
  it("retrieves errors for a file and uri", () => {});
  it("clears errors", () => {});
});
